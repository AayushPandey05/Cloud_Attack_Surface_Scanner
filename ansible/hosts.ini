# hosts.ini - list of servers Ansible will connect to
# Replace the IP below with your actual server IP

[my_servers]
192.168.1.100   ansible_user=ubuntu   ansible_ssh_private_key_file=~/.ssh/id_rsa

# For local testing (runs on your own machine):
# localhost ansible_connection=local
